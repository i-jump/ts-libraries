var n = function(n) {
  return Math.min.apply(null, n);
};
function t(n) {
  return n.b;
}
var r = function(n, t) {
    void 0 === t && (t = 0);
    for (
      var r = Math.pow(10, t),
        l = (Math.round(n * r) / r).toString(),
        i = l.split('.')[1] ? l.split('.')[1].length : 0;
      i < t;

    )
      (l += l.includes('.') ? '0' : '.0'), i++;
    return l;
  },
  l = function(n, t) {
    return (
      void 0 === t && (t = 'all'),
      n.replace(
        {
          all: /\s*/g,
          around: /(^\s*)|(\s*$)/g,
          left: /(^\s*)/g,
          right: /(\s*$)/g,
        }[t],
        ''
      )
    );
  };
export { t as demoFun, r as demoRound, l as demoTrim, n as minArray };
//# sourceMappingURL=ts-lib.cjs.production.min.js.map
