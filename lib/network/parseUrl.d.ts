/**
 * 根据传入规则自动补全接口项目名
 *
 * @param {Array} rules 需要排除的项目名集合
 * @param {String} target 需要补全的项目名
 * @return 二阶函数
 */
declare const _default: (rules: string[], target: string) => Function;
export default _default;
